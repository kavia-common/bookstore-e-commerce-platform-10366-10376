# Public keys only. Do NOT commit real secrets.
# Stripe publishable key for client-side Stripe SDK
REACT_APP_STRIPE_PUBLISHABLE_KEY=pk_test_replace_me

# Backend API base URL (e.g., http://localhost:8000/api or /api when proxied)
REACT_APP_API_BASE_URL=/api
